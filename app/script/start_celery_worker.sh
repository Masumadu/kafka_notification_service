#!/bin/bash

# terminate script if any command fails
set -o errexit

# terminate script if any pipe command fails
#set -o pipefail

# terminate script if any variable being used is unset
set -o nounset